import 'package:flutter/material.dart';



class StateWithProvider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('状態管理: provider')),
      body: Text('状態管理のテスト画面'),
    );
  }

}
